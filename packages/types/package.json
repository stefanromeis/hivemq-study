{
  "name": "@hivemq-study/types",
  "version": "0.0.0",
  "private": true,
  "description": "Shared Zod schemas for MQTT payloads and HTTP contracts. Single source of truth across frontend and (future) Node backends.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./mqtt": "./src/mqtt.ts",
    "./api": "./src/api.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@hivemq-study/config-eslint": "workspace:*",
    "@hivemq-study/config-typescript": "workspace:*",
    "typescript": "^5.6.2",
    "vitest": "^2.1.2"
  }
}
